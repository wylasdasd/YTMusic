@using MudBlazor

<style>
    .confirm-remove-dialog .mud-dialog-actions {
        flex-direction: column;
        gap: 8px;
    }
    .confirm-remove-dialog .mud-dialog-actions .mud-button {
        margin: 0;
    }
</style>

<MudDialog Class="confirm-remove-dialog">
    <DialogContent>
        <MudText Typo="Typo.body1">
            确定要取消收藏「@TrackTitle」吗？
        </MudText>
        @if (IsDownloaded)
        {
            <MudText Typo="Typo.body2" Color="Color.Secondary" Class="mt-2">
                本地已下载文件可仅移出收藏夹，或同时删除文件。
            </MudText>
        }
    </DialogContent>
    <DialogActions>
        @if (IsDownloaded)
        {
            <MudButton OnClick="UnfavoriteAndDelete" Color="Color.Error" Variant="Variant.Filled" FullWidth="true">取消收藏并删除文件</MudButton>
            <MudButton OnClick="UnfavoriteOnly" Color="Color.Primary" Variant="Variant.Outlined" FullWidth="true">仅取消收藏</MudButton>
            <MudButton OnClick="Cancel" Color="Color.Default" FullWidth="true">取消</MudButton>
        }
        else
        {
            <MudButton OnClick="UnfavoriteOnly" Color="Color.Error" Variant="Variant.Filled" FullWidth="true">取消收藏</MudButton>
            <MudButton OnClick="Cancel" Color="Color.Default" FullWidth="true">取消</MudButton>
        }
    </DialogActions>
</MudDialog>

@code {
  [CascadingParameter] private IMudDialogInstance MudDialog { get; set; } = default!;

  [Parameter] public string TrackTitle { get; set; } = string.Empty;

  [Parameter] public bool IsDownloaded { get; set; }

  private void Cancel()
  {
    MudDialog.Cancel();
  }

  private void UnfavoriteOnly()
  {
    MudDialog.Close(DialogResult.Ok("unfavorite"));
  }

  private void UnfavoriteAndDelete()
  {
    MudDialog.Close(DialogResult.Ok("delete"));
  }
}
